import type { CSSProperties } from "react";
import { AbsoluteFill, interpolate, spring, useCurrentFrame, useVideoConfig } from "remotion";
import { arenaTheme } from "../theme";

export const AGENT_LOADOUT_DURATION_IN_FRAMES = 360;

type ToolKind = "terminal" | "git" | "search" | "hammer" | "hex" | "scan" | "fuzz" | "brackets" | "cast";

type CatalogItem = {
  name: string;
  group: string;
  cost: number;
  latency: number;
  kind: ToolKind;
  color: string;
  description: string;
  modules: string[];
  stats: [number, number, number, number];
};

const catalog: CatalogItem[] = [
  {
    name: "BASH",
    group: "EXECUTION",
    cost: 250,
    latency: 8,
    kind: "terminal",
    color: arenaTheme.cyan,
    description: "Direct shell execution for local inspection, scripting, and process control.",
    modules: ["SHELL", "PIPES", "PROCESS"],
    stats: [98, 72, 58, 96],
  },
  {
    name: "GIT",
    group: "SOURCE CONTROL",
    cost: 300,
    latency: 11,
    kind: "git",
    color: "#67e8f9",
    description: "Read history, compare patches, trace regressions, and preserve a clean attack path.",
    modules: ["DIFF", "BLAME", "HISTORY"],
    stats: [92, 78, 70, 98],
  },
  {
    name: "SEARCH",
    group: "RECON",
    cost: 220,
    latency: 6,
    kind: "search",
    color: "#93c5fd",
    description: "Fast symbol, pattern, and dependency search across the complete challenge workspace.",
    modules: ["TEXT", "SYMBOL", "PATH"],
    stats: [96, 83, 65, 94],
  },
  {
    name: "FOUNDRY SUITE",
    group: "BUILD + TEST",
    cost: 1200,
    latency: 38,
    kind: "hammer",
    color: arenaTheme.yellow,
    description: "Compile, test, trace, fork, and interact with EVM contracts in one deterministic toolkit.",
    modules: ["FORGE", "CAST", "ANVIL", "CHISEL"],
    stats: [94, 91, 88, 93],
  },
  {
    name: "SOLC MATRIX",
    group: "COMPILER",
    cost: 650,
    latency: 24,
    kind: "hex",
    color: "#d4d4d8",
    description: "Switch compiler versions and inspect artifacts, storage layout, and generated bytecode.",
    modules: ["AST", "ABI", "BYTECODE"],
    stats: [86, 76, 92, 99],
  },
  {
    name: "SLITHER",
    group: "STATIC ANALYSIS",
    cost: 1050,
    latency: 31,
    kind: "scan",
    color: arenaTheme.green,
    description: "Run focused Solidity detectors and structural printers before deeper exploit work.",
    modules: ["DETECT", "PRINT", "DATAFLOW"],
    stats: [90, 96, 76, 88],
  },
  {
    name: "MEDUSA",
    group: "FUZZING",
    cost: 1150,
    latency: 54,
    kind: "fuzz",
    color: "#f472b6",
    description: "Coverage-guided parallel fuzzing for properties, assertions, and hostile call sequences.",
    modules: ["CORPUS", "COVERAGE", "WORKERS"],
    stats: [72, 98, 95, 84],
  },
  {
    name: "SEMGREP",
    group: "PATTERN SCAN",
    cost: 780,
    latency: 19,
    kind: "brackets",
    color: "#c084fc",
    description: "Apply fast code patterns to surface dangerous flows and repeated implementation mistakes.",
    modules: ["RULES", "TAINT", "MATCH"],
    stats: [95, 84, 73, 89],
  },
  {
    name: "CAST OPS",
    group: "CHAIN INTEL",
    cost: 520,
    latency: 16,
    kind: "cast",
    color: "#fb923c",
    description: "Decode calldata, inspect chain state, and reproduce contract interactions from the terminal.",
    modules: ["CALL", "SEND", "DECODE"],
    stats: [93, 81, 79, 95],
  },
];

const loadout = [
  { slot: "CORE 01", name: "BASH", type: "TOOL", color: arenaTheme.cyan },
  { slot: "CORE 02", name: "GIT", type: "TOOL", color: "#67e8f9" },
  { slot: "BUILD", name: "FOUNDRY", type: "TOOL", color: arenaTheme.yellow },
  { slot: "AUDIT", name: "SLITHER", type: "TOOL", color: arenaTheme.green },
  { slot: "RELAY 01", name: "GITHUB MCP", type: "MCP", color: "#e5e7eb" },
  { slot: "RELAY 02", name: "CONTEXT7", type: "MCP", color: "#a78bfa" },
  { slot: "SKILL", name: "REENTRANCY HUNTER", type: "SKILL", color: "#f472b6" },
  { slot: "TACTIC", name: "PROOF OF EXPLOIT", type: "TACTIC", color: "#fb923c" },
];

const clamp = { extrapolateLeft: "clamp", extrapolateRight: "clamp" } as const;

const panel: CSSProperties = {
  background: "linear-gradient(155deg, rgba(4,20,24,0.97), rgba(0,8,11,0.94))",
  border: "1px solid rgba(0,251,255,0.2)",
  boxShadow: "inset 0 0 34px rgba(0,251,255,0.025), 0 18px 42px rgba(0,0,0,0.38)",
};

const ToolGlyph = ({ kind, color, size = 64 }: { kind: ToolKind; color: string; size?: number }) => {
  const common = { fill: "none", stroke: color, strokeWidth: 2.2, strokeLinecap: "round" as const, strokeLinejoin: "round" as const };

  return (
    <svg width={size} height={size} viewBox="0 0 64 64" aria-hidden="true">
      {kind === "terminal" ? (
        <>
          <rect x="7" y="11" width="50" height="42" rx="3" {...common} />
          <path d="M17 25l8 7-8 7M31 40h14" {...common} />
        </>
      ) : null}
      {kind === "git" ? (
        <>
          <path d="M20 14v27c0 7 5 10 12 10s12-3 12-10V29" {...common} />
          <circle cx="20" cy="13" r="5" {...common} />
          <circle cx="44" cy="25" r="5" {...common} />
          <circle cx="32" cy="51" r="5" {...common} />
          <path d="M20 28h10c8 0 14-5 14-12" {...common} />
        </>
      ) : null}
      {kind === "search" ? (
        <>
          <circle cx="28" cy="28" r="16" {...common} />
          <path d="M40 40l14 14M21 28h14M28 21v14" {...common} />
        </>
      ) : null}
      {kind === "hammer" ? (
        <>
          <path d="M17 14h24l7 8-8 8-8-7-18 29-7-5 18-29z" {...common} />
          <path d="M39 13l8 9" {...common} />
        </>
      ) : null}
      {kind === "hex" ? (
        <>
          <path d="M32 6l23 13v26L32 58 9 45V19z" {...common} />
          <path d="M41 22c-3-3-15-4-17 1-3 7 18 5 15 14-2 6-14 5-18 1" {...common} />
        </>
      ) : null}
      {kind === "scan" ? (
        <>
          <path d="M11 20V9h11M42 9h11v11M53 44v11H42M22 55H11V44" {...common} />
          <path d="M18 34c8-14 20 14 28-2M18 40c9-11 20 9 28-4" {...common} />
          <path d="M15 29h34" {...common} />
        </>
      ) : null}
      {kind === "fuzz" ? (
        <>
          <circle cx="32" cy="28" r="11" {...common} />
          <path d="M21 22L9 14M22 30L7 31M23 37L12 49M43 22l12-8M42 30l15 1M41 37l11 12M27 18l-3-11M37 18l3-11M28 39l-3 16M36 39l3 16" {...common} />
          <circle cx="28" cy="27" r="1.5" fill={color} />
          <circle cx="36" cy="27" r="1.5" fill={color} />
        </>
      ) : null}
      {kind === "brackets" ? (
        <>
          <path d="M24 10H12v44h12M40 10h12v44H40" {...common} />
          <path d="M38 17L26 47M20 26l-7 6 7 6M44 26l7 6-7 6" {...common} />
        </>
      ) : null}
      {kind === "cast" ? (
        <>
          <path d="M32 7l20 12v25L32 57 12 44V19zM12 19l20 12 20-12M32 31v26" {...common} />
          <path d="M22 14l20 12" {...common} />
        </>
      ) : null}
    </svg>
  );
};

const CornerMarks = () => (
  <>
    {["top-left", "top-right", "bottom-left", "bottom-right"].map((corner) => (
      <div
        key={corner}
        style={{
          position: "absolute",
          width: 18,
          height: 18,
          top: corner.startsWith("top") ? 11 : undefined,
          bottom: corner.startsWith("bottom") ? 11 : undefined,
          left: corner.endsWith("left") ? 11 : undefined,
          right: corner.endsWith("right") ? 11 : undefined,
          borderTop: corner.startsWith("top") ? `2px solid ${arenaTheme.cyan}` : undefined,
          borderBottom: corner.startsWith("bottom") ? `2px solid ${arenaTheme.cyan}` : undefined,
          borderLeft: corner.endsWith("left") ? `2px solid ${arenaTheme.cyan}` : undefined,
          borderRight: corner.endsWith("right") ? `2px solid ${arenaTheme.cyan}` : undefined,
          opacity: 0.48,
        }}
      />
    ))}
  </>
);

const StatBar = ({ label, value, color }: { label: string; value: number; color: string }) => (
  <div style={{ marginBottom: 15 }}>
    <div style={{ display: "flex", justifyContent: "space-between", color: "rgba(245,255,255,0.62)", fontSize: 11, fontWeight: 900, letterSpacing: "0.11em" }}>
      <span>{label}</span>
      <span style={{ color: arenaTheme.white }}>{value}</span>
    </div>
    <div style={{ height: 5, marginTop: 7, background: "rgba(245,255,255,0.08)", overflow: "hidden" }}>
      <div style={{ width: `${value}%`, height: "100%", background: color, boxShadow: `0 0 12px ${color}` }} />
    </div>
  </div>
);

const AgentBadge = () => (
  <div style={{ width: 92, height: 92, borderRadius: 999, display: "flex", alignItems: "center", justifyContent: "center", color: arenaTheme.cyan, border: `2px solid ${arenaTheme.cyan}`, background: "radial-gradient(circle, rgba(0,251,255,0.18), rgba(0,8,11,0.84) 68%)", boxShadow: "0 0 30px rgba(0,251,255,0.2)", fontSize: 32, fontWeight: 950 }}>
    OA
  </div>
);

export type AgentLoadoutProps = {
  hideLock?: boolean;
};

export const AgentLoadout = ({ hideLock = false }: AgentLoadoutProps) => {
  const frame = useCurrentFrame();
  const { fps } = useVideoConfig();
  const intro = spring({ frame, fps, config: { damping: 200 } });
  const catalogIn = spring({ frame: frame - 8, fps, config: { damping: 200 } });
  const loadoutIn = spring({ frame: frame - 20, fps, config: { damping: 200 } });
  const selectedIndex = frame < 56 ? 0 : frame < 92 ? 1 : frame < 128 ? 2 : frame < 172 ? 3 : frame < 208 ? 5 : frame < 244 ? 6 : frame < 282 ? 7 : 3;
  const selected = catalog[selectedIndex];
  const lockProgress = hideLock ? 0 : interpolate(frame, [9.5 * fps, 10.3 * fps], [0, 1], clamp);
  const pulse = 0.72 + Math.sin(frame / 5) * 0.18;
  const scanY = (frame * 4) % 1080;

  return (
    <AbsoluteFill style={{ overflow: "hidden", background: arenaTheme.background, color: arenaTheme.white, fontFamily: '"Courier New", ui-monospace, monospace' }}>
      <AbsoluteFill
        style={{
          background:
            "radial-gradient(circle at 50% 38%, rgba(0,251,255,0.08), transparent 35%), radial-gradient(circle at 11% 76%, rgba(0,255,156,0.06), transparent 25%), linear-gradient(140deg, #020b0e, #000507 58%, #071015)",
        }}
      />
      <AbsoluteFill style={{ opacity: 0.16, backgroundImage: "linear-gradient(rgba(0,251,255,0.2) 1px, transparent 1px), linear-gradient(90deg, rgba(0,251,255,0.2) 1px, transparent 1px)", backgroundSize: "48px 48px", transform: `translateY(${(frame * 0.35) % 48}px)` }} />
      <div style={{ position: "absolute", left: 0, right: 0, top: scanY, height: 2, opacity: 0.11, background: arenaTheme.cyan, boxShadow: `0 0 18px ${arenaTheme.cyan}` }} />

      <div style={{ position: "absolute", inset: 24, opacity: intro, transform: `scale(${0.985 + intro * 0.015})` }}>
        <div style={{ ...panel, position: "absolute", left: 0, right: 0, top: 0, height: 78, display: "grid", gridTemplateColumns: "1fr 420px 1fr", alignItems: "center", padding: "0 25px", clipPath: "polygon(0 0, 100% 0, 98.8% 100%, 1.2% 100%)" }}>
          <div>
            <div style={{ color: arenaTheme.cyan, fontSize: 12, fontWeight: 950, letterSpacing: "0.23em" }}>AGENTS ARENA // PRE-RUN</div>
            <div style={{ marginTop: 6, fontSize: 26, fontWeight: 950, letterSpacing: "0.06em" }}>THE ARMORY</div>
          </div>
          <div style={{ textAlign: "center" }}>
            <div style={{ color: arenaTheme.red, fontSize: 10, fontWeight: 950, letterSpacing: "0.26em" }}>LOADOUT PHASE</div>
            <div style={{ marginTop: 3, fontSize: 35, lineHeight: 1, fontWeight: 950, letterSpacing: "0.11em" }}>00:18</div>
          </div>
          <div style={{ display: "flex", justifyContent: "flex-end", gap: 30, textAlign: "right" }}>
            <div>
              <div style={{ color: "rgba(245,255,255,0.42)", fontSize: 10, fontWeight: 900, letterSpacing: "0.13em" }}>COMPUTE BUDGET</div>
              <div style={{ marginTop: 5, color: arenaTheme.yellow, fontSize: 25, fontWeight: 950 }}>8,000 CU</div>
            </div>
            <div>
              <div style={{ color: "rgba(245,255,255,0.42)", fontSize: 10, fontWeight: 900, letterSpacing: "0.13em" }}>SLOTS</div>
              <div style={{ marginTop: 5, color: arenaTheme.green, fontSize: 25, fontWeight: 950 }}>8 / 8</div>
            </div>
          </div>
        </div>

        <div style={{ position: "absolute", left: 0, right: 0, top: 92, height: 610, display: "grid", gridTemplateColumns: "292px minmax(0, 1fr) 358px", gap: 12 }}>
          <div style={{ ...panel, position: "relative", padding: 20, overflow: "hidden" }}>
            <CornerMarks />
            <div style={{ display: "flex", gap: 16, alignItems: "center" }}>
              <AgentBadge />
              <div>
                <div style={{ color: arenaTheme.red, fontSize: 10, fontWeight: 950, letterSpacing: "0.18em" }}>P3 // ATTACKER</div>
                <div style={{ marginTop: 8, fontSize: 22, lineHeight: 0.95, fontWeight: 950 }}>GPT-5.5</div>
                <div style={{ marginTop: 8, color: arenaTheme.cyan, fontSize: 12, fontWeight: 900 }}>CODEX // HIGH</div>
              </div>
            </div>

            <div style={{ marginTop: 24, paddingTop: 18, borderTop: "1px solid rgba(0,251,255,0.16)" }}>
              <div style={{ display: "flex", justifyContent: "space-between", color: "rgba(245,255,255,0.48)", fontSize: 10, fontWeight: 900, letterSpacing: "0.12em" }}><span>UNSPENT</span><span style={{ color: arenaTheme.yellow }}>2,530 CU</span></div>
              <div style={{ height: 6, marginTop: 9, background: "rgba(245,255,255,0.07)" }}><div style={{ width: "68%", height: "100%", background: arenaTheme.cyan }} /></div>
              <div style={{ marginTop: 12, display: "flex", justifyContent: "space-between", color: "rgba(245,255,255,0.48)", fontSize: 10, fontWeight: 900, letterSpacing: "0.12em" }}><span>LATENCY CAP</span><span style={{ color: arenaTheme.green }}>186 / 300 MS</span></div>
            </div>

            <div style={{ marginTop: 23, color: "rgba(245,255,255,0.35)", fontSize: 9, fontWeight: 950, letterSpacing: "0.18em" }}>LOADOUT SYSTEMS</div>
            {[
              ["01", "TOOLCHAIN", "SELECTED", arenaTheme.cyan],
              ["02", "MCP RELAYS", "2 LINKED", "#a78bfa"],
              ["03", "SKILL DECK", "1 ACTIVE", "#f472b6"],
              ["04", "TACTICS", "1 ACTIVE", "#fb923c"],
            ].map(([number, label, status, color], index) => (
              <div key={label} style={{ position: "relative", marginTop: 9, height: 58, padding: "0 13px", display: "flex", alignItems: "center", justifyContent: "space-between", color: index === 0 ? arenaTheme.white : "rgba(245,255,255,0.58)", background: index === 0 ? "rgba(0,251,255,0.11)" : "rgba(245,255,255,0.025)", border: `1px solid ${index === 0 ? "rgba(0,251,255,0.65)" : "rgba(245,255,255,0.08)"}` }}>
                <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
                  <span style={{ color: color as string, fontSize: 11, fontWeight: 950 }}>{number}</span>
                  <span style={{ fontSize: 14, fontWeight: 950, letterSpacing: "0.06em" }}>{label}</span>
                </div>
                <span style={{ color: color as string, fontSize: 8, fontWeight: 950, letterSpacing: "0.08em" }}>{status}</span>
              </div>
            ))}

            <div style={{ position: "absolute", left: 20, right: 20, bottom: 20, padding: "12px 13px", color: "rgba(245,255,255,0.58)", background: "rgba(255,88,97,0.07)", border: "1px solid rgba(255,88,97,0.24)", fontSize: 9, lineHeight: 1.45, fontWeight: 900, letterSpacing: "0.08em" }}>
              NEUTRAL RULESET<br /><span style={{ color: arenaTheme.red }}>SAME BUDGET. SAME SANDBOX. NO COMMS.</span>
            </div>
          </div>

          <div style={{ ...panel, position: "relative", padding: "17px 18px 16px", opacity: catalogIn, transform: `translateY(${(1 - catalogIn) * 18}px)` }}>
            <div style={{ height: 39, display: "flex", alignItems: "flex-start", justifyContent: "space-between" }}>
              <div>
                <span style={{ color: arenaTheme.cyan, fontSize: 12, fontWeight: 950, letterSpacing: "0.17em" }}>TOOLCHAIN</span>
                <span style={{ marginLeft: 14, color: "rgba(245,255,255,0.32)", fontSize: 9, fontWeight: 900, letterSpacing: "0.11em" }}>EXECUTE // BUILD // TEST // AUDIT</span>
              </div>
              <div style={{ color: "rgba(245,255,255,0.38)", fontSize: 9, fontWeight: 900, letterSpacing: "0.1em" }}>SELECT MODULE TO INSPECT</div>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(3, minmax(0, 1fr))", gap: 9 }}>
              {catalog.map((item, index) => {
                const selectedCard = index === selectedIndex;
                const cardIn = spring({ frame: frame - 12 - index * 2, fps, config: { damping: 200 } });
                return (
                  <div key={item.name} style={{ position: "relative", height: 166, padding: "12px 13px", overflow: "hidden", background: selectedCard ? `linear-gradient(145deg, ${item.color}2d, rgba(0,25,29,0.95))` : "linear-gradient(145deg, rgba(8,28,32,0.9), rgba(0,12,15,0.95))", border: `1px solid ${selectedCard ? item.color : "rgba(245,255,255,0.1)"}`, boxShadow: selectedCard ? `inset 0 0 28px ${item.color}22, 0 0 17px ${item.color}18` : "none", opacity: cardIn, transform: `scale(${0.96 + cardIn * 0.04})` }}>
                    {selectedCard ? <div style={{ position: "absolute", inset: 3, border: `1px solid ${item.color}55`, pointerEvents: "none" }} /> : null}
                    <div style={{ display: "flex", justifyContent: "space-between", color: "rgba(245,255,255,0.42)", fontSize: 8, fontWeight: 950, letterSpacing: "0.1em" }}><span>{item.group}</span><span style={{ color: item.color }}>{String(index + 1).padStart(2, "0")}</span></div>
                    <div style={{ height: 86, display: "flex", alignItems: "center", justifyContent: "center", opacity: selectedCard ? 1 : 0.62 }}><ToolGlyph kind={item.kind} color={selectedCard ? item.color : "#9fb5b8"} size={72} /></div>
                    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end" }}>
                      <div style={{ maxWidth: "70%", color: selectedCard ? arenaTheme.white : "rgba(245,255,255,0.72)", fontSize: 14, lineHeight: 1, fontWeight: 950 }}>{item.name}</div>
                      <div style={{ color: item.color, fontSize: 13, fontWeight: 950 }}>{item.cost}<span style={{ marginLeft: 3, fontSize: 8 }}>CU</span></div>
                    </div>
                  </div>
                );
              })}
            </div>
          </div>

          <div style={{ ...panel, position: "relative", padding: 20, overflow: "hidden" }}>
            <CornerMarks />
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
              <div>
                <div style={{ color: selected.color, fontSize: 9, fontWeight: 950, letterSpacing: "0.17em" }}>{selected.group}</div>
                <div style={{ marginTop: 8, fontSize: 27, lineHeight: 0.95, fontWeight: 950 }}>{selected.name}</div>
              </div>
              <div style={{ color: selected.color, textAlign: "right" }}>
                <div style={{ fontSize: 24, fontWeight: 950 }}>{selected.cost}</div>
                <div style={{ fontSize: 8, fontWeight: 950, letterSpacing: "0.12em" }}>COMPUTE UNITS</div>
              </div>
            </div>

            <div style={{ height: 122, marginTop: 16, display: "flex", alignItems: "center", justifyContent: "center", background: `radial-gradient(circle, ${selected.color}1f, transparent 65%)`, borderTop: "1px solid rgba(245,255,255,0.06)", borderBottom: "1px solid rgba(245,255,255,0.06)" }}>
              <ToolGlyph kind={selected.kind} color={selected.color} size={105} />
            </div>

            <div style={{ minHeight: 57, marginTop: 15, color: "rgba(245,255,255,0.62)", fontSize: 11, lineHeight: 1.5 }}>{selected.description}</div>
            <div style={{ marginTop: 8, display: "flex", gap: 6, flexWrap: "wrap" }}>
              {selected.modules.map((module) => <span key={module} style={{ padding: "5px 7px", color: selected.color, background: `${selected.color}10`, border: `1px solid ${selected.color}38`, fontSize: 8, fontWeight: 950, letterSpacing: "0.1em" }}>{module}</span>)}
            </div>

            <div style={{ marginTop: 18 }}>
              <StatBar label="EXECUTION SPEED" value={selected.stats[0]} color={selected.color} />
              <StatBar label="FINDING COVERAGE" value={selected.stats[1]} color={selected.color} />
              <StatBar label="REASONING LOAD" value={selected.stats[2]} color={selected.color} />
              <StatBar label="DETERMINISM" value={selected.stats[3]} color={selected.color} />
            </div>

            <div style={{ position: "absolute", left: 20, right: 20, bottom: 19, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }}>
              <div style={{ padding: "12px 10px", color: "rgba(245,255,255,0.55)", background: "rgba(245,255,255,0.035)", border: "1px solid rgba(245,255,255,0.1)", textAlign: "center", fontSize: 9, fontWeight: 950, letterSpacing: "0.09em" }}>P95 {selected.latency} MS</div>
              <div style={{ padding: "12px 10px", color: arenaTheme.background, background: selected.color, textAlign: "center", fontSize: 9, fontWeight: 950, letterSpacing: "0.09em" }}>EQUIPPED ✓</div>
            </div>
          </div>
        </div>

        <div style={{ ...panel, position: "absolute", left: 0, right: 0, top: 716, height: 316, padding: "16px 18px", opacity: loadoutIn, transform: `translateY(${(1 - loadoutIn) * 22}px)` }}>
          <div style={{ height: 35, display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
            <div>
              <span style={{ color: arenaTheme.green, fontSize: 12, fontWeight: 950, letterSpacing: "0.18em" }}>ACTIVE LOADOUT</span>
              <span style={{ marginLeft: 14, color: "rgba(245,255,255,0.34)", fontSize: 9, fontWeight: 900, letterSpacing: "0.11em" }}>8 MODULES // 5,470 CU // 186 MS</span>
            </div>
            <div style={{ color: arenaTheme.yellow, fontSize: 9, fontWeight: 950, letterSpacing: "0.12em" }}>CHALLENGE UNKNOWN // BLIND CONFIGURATION</div>
          </div>

          <div style={{ display: "grid", gridTemplateColumns: "repeat(8, minmax(0, 1fr))", gap: 8 }}>
            {loadout.map((item, index) => {
              const slotIn = spring({ frame: frame - 25 - index * 4, fps, config: { damping: 200 } });
              return (
                <div key={item.slot} style={{ position: "relative", height: 174, padding: "13px 12px", display: "flex", flexDirection: "column", justifyContent: "space-between", color: arenaTheme.white, background: `linear-gradient(155deg, ${item.color}15, rgba(0,10,13,0.96))`, border: `1px solid ${item.color}50`, boxShadow: `inset 0 0 23px ${item.color}0d`, opacity: slotIn, transform: `translateY(${(1 - slotIn) * 14}px)` }}>
                  <div style={{ display: "flex", justifyContent: "space-between", color: "rgba(245,255,255,0.4)", fontSize: 8, fontWeight: 950, letterSpacing: "0.1em" }}><span>{item.slot}</span><span style={{ color: item.color }}>{item.type}</span></div>
                  <div style={{ width: 48, height: 48, margin: "0 auto", display: "flex", alignItems: "center", justifyContent: "center", color: item.color, border: `1px solid ${item.color}88`, background: `${item.color}12`, fontSize: 18, fontWeight: 950 }}>{item.name.slice(0, 2)}</div>
                  <div style={{ minHeight: 30, textAlign: "center", fontSize: item.name.length > 15 ? 10 : 13, lineHeight: 1.15, fontWeight: 950 }}>{item.name}</div>
                  <div style={{ height: 3, background: "rgba(245,255,255,0.07)" }}><div style={{ width: "100%", height: "100%", background: item.color }} /></div>
                </div>
              );
            })}
          </div>

          <div style={{ position: "absolute", left: "50%", bottom: 13, transform: "translateX(-50%)", color: "rgba(245,255,255,0.35)", fontSize: 8, fontWeight: 900, letterSpacing: "0.16em" }}>LOADOUT HASH // 0X7A3F...91C2 // READY TO SEAL</div>
        </div>
      </div>

      {lockProgress > 0 ? (
        <AbsoluteFill style={{ pointerEvents: "none", alignItems: "center", justifyContent: "center", background: `rgba(0,8,11,${lockProgress * 0.56})`, opacity: lockProgress }}>
          <div style={{ width: 760, padding: "31px 38px", color: arenaTheme.green, background: "rgba(0,19,19,0.95)", border: `2px solid ${arenaTheme.green}`, boxShadow: `0 0 ${36 * pulse}px rgba(0,255,156,0.48), inset 0 0 32px rgba(0,255,156,0.08)`, textAlign: "center", clipPath: "polygon(3% 0, 97% 0, 100% 22%, 100% 78%, 97% 100%, 3% 100%, 0 78%, 0 22%)" }}>
            <div style={{ fontSize: 13, fontWeight: 950, letterSpacing: "0.28em" }}>CONFIGURATION SEALED</div>
            <div style={{ marginTop: 13, color: arenaTheme.white, fontSize: 50, lineHeight: 0.95, fontWeight: 950, letterSpacing: "0.055em" }}>LOADOUT LOCKED</div>
            <div style={{ marginTop: 15, color: arenaTheme.cyan, fontSize: 12, fontWeight: 950, letterSpacing: "0.16em" }}>AGENT P3 // ENTERING NEUTRAL SANDBOX</div>
          </div>
        </AbsoluteFill>
      ) : null}
    </AbsoluteFill>
  );
};
