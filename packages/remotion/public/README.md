# Static video assets

Place source assets here and organize them by campaign, for example:

```text
public/
  agent-arena-launch/
    audio/
    images/
    video/
```

Use `staticFile("agent-arena-launch/images/example.png")` from a composition. Keep editable source files here; generated renders belong in `../out/`.
